#!/usr/bin/env node

const path = require('path');
const {mainCommand} = require(path.join(__dirname, '..', 'lib', 'index.js'));

mainCommand().parseAsync(process.argv).catch((error) => {
  console.error(error);
  process.exit(1);
});
