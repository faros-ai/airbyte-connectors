{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "email": {
      "type": "string",
      "description": "User's email address (mapped from api_key)"
    },
    "date": {
      "type": "string",
      "description": "Date in YYYY-MM-DD format"
    },
    "sum_chat_loc_used": {
      "type": ["number", "null"],
      "description": "Sum of lines of code used in chat"
    },
    "model_id": {
      "type": ["string", "null"],
      "description": "Model ID used for chat"
    },
    "ide": {
      "type": ["string", "null"],
      "description": "IDE being used"
    },
    "latest_intent_type": {
      "type": ["string", "null"],
      "description": "Latest intent type of the chat session"
    },
    "sum_num_chats_received": {
      "type": ["number", "null"],
      "description": "Sum of number of chats received"
    }
  },
  "required": [
    "email",
    "date"
  ]
}
