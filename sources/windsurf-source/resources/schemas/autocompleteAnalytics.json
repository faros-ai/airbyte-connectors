{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "email": {
      "type": "string",
      "description": "User's email address (mapped from api_key)"
    },
    "date": {
      "type": "string",
      "description": "Date in YYYY-MM-DD format"
    },
    "sum_num_acceptances": {
      "type": ["number", "null"],
      "description": "Sum of autocomplete suggestions accepted"
    },
    "sum_num_lines_accepted": {
      "type": ["number", "null"],
      "description": "Sum of lines accepted from autocomplete"
    },
    "language": {
      "type": ["string", "null"],
      "description": "Programming language"
    },
    "ide": {
      "type": ["string", "null"],
      "description": "IDE being used"
    }
  },
  "required": [
    "email",
    "date"
  ]
}
