{
  "documentationUrl": "https://docs.faros.ai",
  "connectionSpecification": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Jenkins Spec",
    "type": "object",
    "required": ["server_url", "user", "token"],
    "additionalProperties": true,
    "properties": {
      "username": {
        "type": "string",
        "title": "TestRails Username"
      },
      "api_key": {
        "type": "string",
        "title": "Test Rails API Key",
        "airbyte_secret": true
      },
      "instance_url": {
        "type": "string",
        "title": "TestRails Instance URL",
        "examples": ["https://example.testrail.com"]
      },
      "project_names": {
        "type": "string",
        "title": "TestRails Projects to Sync",
        "description": "If no Spaces provided, all Spaces will be synced.",
        "items": {
          "type": "string"
        }
      },
      "cutoff_days": {
        "type": "number",
        "title": "Cutoff Days",
        "description": "The number of days of data to sync from Octopus.",
        "default": 90
      },
      "page_size": {
        "type": "number",
        "title": "Page Size",
        "description": "The page size to use when paginating Octopus resources.",
        "default": 100
      },
      "max_retries": {
        "type": "number",
        "title": "Max Retries",
        "description": "The maximum number of times to retry requests to Octopus.",
        "default": 3
      },
      "reject_unauthorized": {
        "type": "boolean",
        "title": "Reject Unauthorized",
        "description": "Enable certificate validation for the Octopus server.",
        "default": true
      },
      "before": {
        "type": "string",
        "title": "Sync to Date",
        "examples": ["2023-06-20"]
      },
      "after": {
        "type": "string",
        "title": "Sync from Date",
        "examples": ["2023-06-20"],
        "description": "When provided with 'Sync to Date', only data from within the time window will be considered."
      }
    }
  }
}
