{
  "documentationUrl": "https://developers.linear.app/docs/",
  "connectionSpecification": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Linear Spec",
    "type": "object",
    "required": ["api_key"],
    "additionalProperties": false,
    "properties": {
      "api_key": {
        "type": "string",
        "title": "API Key",
        "description": "For Linear Plus plan subscription: https://linear.app/vadimeo/settings/integrations/airbyte,for public api: create api_key https://linear.app/{team}/settings/api",
        "airbyte_secret": true
      },
      "api_type": {
        "type": "string",
        "title": "API Type",
        "description": "paid - only available to the Plus paid plan, public - Linear's official public API",
        "default": "paid"
      },
      "page_size": {
        "type": "number",
        "title": "Page Size",
        "description": "number of issues to fetch per call"
      },
      "cutoff_days": {
        "type": "integer",
        "title": "Cutoff Days",
        "default": 90,
        "description": "Only fetch data updated after cutoff"
      }
    }
  }
}
