{
  "documentationUrl": "https://docs.faros.ai",
  "connectionSpecification": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "ClickUp Spec",
    "type": "object",
    "required": ["token", "cutoff_days"],
    "additionalProperties": true,
    "properties": {
      "token": {
        "type": "string",
        "title": "API Token",
        "description": "ClickUp API Token. Follow the instructions at https://clickup.com/api/developer-portal/authentication#generate-your-personal-api-token to generate this token.",
        "airbyte_secret": true
      },
      "fetch_archived": {
        "type": "boolean",
        "title": "Fetch Archived Data",
        "description": "Fetch archived spaces, folders, lists, and tasks",
        "default": false
      },
      "fetch_subtasks": {
        "type": "boolean",
        "title": "Fetch Subtasks",
        "description": "Fetch subtasks in addition to regular tasks",
        "default": false
      },
      "cutoff_days": {
        "type": "integer",
        "title": "Cutoff Days",
        "default": 90,
        "description": "Only fetch data updated after cutoff"
      },
      "timeout": {
        "type": "integer",
        "minimum": 1000,
        "default": 60000,
        "title": "Timeout",
        "description": "Request timeout in milliseconds"
      }
    }
  }
}
